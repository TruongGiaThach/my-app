import { SafeAreaView, StyleSheet, Text, View } from "react-native";

const LotsOfStyles = StyleSheet.create({
    container: {
      marginTop: 50,
    },
    bigBlue: {
      color: 'blue',
      fontWeight: 'bold',
      fontSize: 30,
    },
    red: {
      color: 'red',
    },
  });
  
export default LotsOfStyles;